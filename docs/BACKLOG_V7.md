# aimedia.global v7 backlog

Deferred from v6.2 (2026-07-16). Each entry is blocked on inputs that do not
exist yet or on a structural decision that deserves its own spec. Nothing here
ships without NAYL's explicit go.

## 1. Work showcase / portfolio section

A real "Work" section with project frames, before/after system shots, and
deliverable walkthroughs.

- Blocked on: NDA-cleared client assets. Current engagements cannot be shown
  publicly until clients sign off on what is shareable.
- Until then the Outcomes section stays anonymized (industry + qualitative
  result), which is the halal-safe default.
- When unblocked: reuse the ShowcaseMedia frame component; the nav "Work" link
  already exists and points at #ai-team as a placeholder.

## 2. NFT / Web3 case studies (XWECAN and peers)

Named Web3 case studies with launch-window numbers (community mapped, leads
contacted, mint outcomes).

- Blocked on: halal-verifiable numbers only. Every figure must trace to a
  source we can show on a call (exports, dashboards), no reconstructed or
  estimated stats presented as fact.
- Scope: one case template (challenge, system built, traceable outcome),
  starting with XWECAN, then the next two strongest Web3 engagements.

## 3. Testimonials row

Real, attributed quotes from clients (name, role, company or anonymized role
plus industry where NDA requires).

- Blocked on: collecting 3 to 5 genuine quotes. No placeholder or paraphrased
  testimonials in the meantime; an empty section is better than an invented one.
- Ask happens at natural moments: post-delivery reviews and renewal calls.

## 4. Multi-level story / scroll narrative

Restructure the page from a section stack into a layered narrative (chaptered
scroll, persistent progress, deeper second-level pages per engine).

- This is a big information-architecture change, not a polish pass. It needs
  its own spec: content model, routing, scroll choreography, performance
  budget, and how the booking funnel survives the restructure.
- Do not start as a side effect of another workstream.
